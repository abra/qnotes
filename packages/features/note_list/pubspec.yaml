name: note_list
description: "Note list feature: displays, searches and manages the list of notes."
version: 0.0.1

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
